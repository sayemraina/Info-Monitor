# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/

# Environment
.env

# Regenerated data outputs — now committed for Vercel deployment.
# Previously gitignored to avoid context window bloat, but Vercel needs these files.
# Update: all metric/geo/discourse/signals data now tracked in git.

# Synthetic data backup — preserved for reference/demo mode
data_synthetic/

# Real-time YouTube discovery output — now committed so infomonitor.app shows fresh narrative shapers
# data/youtube/live/  ← un-gitignored

# Pipeline timing logs (JSONL, append-only)
data/.timings/

# AI Guide audit logs (JSONL, interaction history)
data/.ai_guide/

# public/data now contains real files (was a symlink before)
